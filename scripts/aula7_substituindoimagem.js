let lista = document.querySelectorAll('img')

/*listaFelinos.forEach(img => {

    
    let atributoCapturado = img.getAttribute("src");   
    
});

    let cardTigre = listaFelinos[0].parentElement;
    cardTigre.appendChild(b);



//let trocarImagem = prompt("Qual imagem você quer alterar")*/

/*let cards = [
{for (let felino of cards) {
    
}}
]*/

let imagens = [lista[0], lista[1], lista[2], lista[3], lista[4], lista[5]]


for (let imagem of imagens) {
    let button = document.createElement("button");
    button.innerText = "Trocar imagem";
    
}